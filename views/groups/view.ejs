<%- include('../partials/head') %>
<%- include('../partials/sidebar') %>
<main class="main-content">
  <%- include('../partials/header') %>
  <div class="content-area">
    <nav class="breadcrumb">
      <span class="breadcrumb-item"><a href="/">Dashboard</a></span>
      <span class="breadcrumb-item"><a href="/groups">Groups</a></span>
      <span class="breadcrumb-item active"><%= group.name %></span>
    </nav>

    <%
      const teacherFullName = teacher ? (teacher.firstName + ' ' + teacher.lastName).toUpperCase() : '';
      const courseName = course ? course.name : '';
      const coursePrice = course ? (course.price || 0) : 0;
      const groupDays = group.days && group.days.length > 0 ? group.days.join(', ') : '--';
      const shortId = group.id.substring(0, 6);
      // End date
      let computedEndDate = endDate || '';
      // Format dates for display
      function fmtDate(ds) {
        if (!ds) return '';
        var d = new Date(ds);
        return String(d.getDate()).padStart(2,'0') + '.' + String(d.getMonth()+1).padStart(2,'0') + '.' + d.getFullYear();
      }
    %>

    <!-- Page Title -->
    <div class="gv-page-title">
      <h1><%= group.name %> &middot; <%= courseName %> &middot; <%= teacherFullName %></h1>
    </div>

    <!-- Show/Hide Coins Toggle -->
    <div class="gv-coins-toggle">
      <span>Show coins</span>
      <label class="gv-toggle-switch">
        <input type="checkbox">
        <span class="gv-toggle-slider"></span>
      </label>
      <span>Hide coins</span>
    </div>

    <div class="gv-layout">
      <!-- ============ LEFT PANEL ============ -->
      <div class="gv-left">
        <div class="gv-info-card">
          <div class="gv-info-content">
            <div class="gv-info-rows">
              <div class="gv-info-row"><span class="gv-info-label">Course:</span> <span class="gv-info-value"><%= courseName || '--' %></span></div>
              <div class="gv-info-row"><span class="gv-info-label">Teacher:</span> <span class="gv-info-value"><%= teacherFullName || '--' %></span></div>
              <div class="gv-info-row"><span class="gv-info-label">Price:</span> <span class="gv-info-value"><%= coursePrice.toLocaleString() %> <%= currency %></span></div>
              <div class="gv-info-row"><span class="gv-info-label">Time:</span> <span class="gv-info-value"><%= groupDays %> &middot; <%= group.startTime || '--' %></span></div>
              <div class="gv-info-row"><span class="gv-info-label">Rooms:</span> <span class="gv-info-value"><%= group.room || '--' %></span></div>
              <div class="gv-info-row"><span class="gv-info-label">Status:</span> <span class="gv-info-value"><%= (group.status || 'active').toUpperCase() %></span></div>
              <div class="gv-info-row"><span class="gv-info-label">Students:</span> <span class="gv-info-value"><%= students.length %></span></div>
              <div class="gv-info-row">
                <span class="gv-info-label">Training dates:</span><br>
                <span class="gv-info-value gv-training-dates"><%= fmtDate(group.startDate) %> &mdash; <%= fmtDate(computedEndDate) %></span>
              </div>
              <div class="gv-info-id">(id: <%= shortId %>)</div>
            </div>
            <div class="gv-info-actions">
              <button type="button" class="gv-action-icon gv-icon-edit" title="Edit" onclick="openEditGroupModal()">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 3a2.83 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z"/></svg>
              </button>
              <form action="/groups/delete/<%= group.id %>" method="POST" class="inline-form" onsubmit="return showDeleteModal(event, '<%= group.name.replace(/'/g, "\\'") %>')">
                <button type="submit" class="gv-action-icon gv-icon-delete" title="Delete">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
                </button>
              </form>
            </div>
          </div>

          <!-- Date of next withdraw -->
          <div class="gv-withdraw-box">
            <div class="gv-withdraw-label">Date of next withdraw:</div>
            <div class="gv-withdraw-date">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
              <%
                // Next withdraw: 1st of next month (billing cycle)
                var nw = new Date();
                nw.setMonth(nw.getMonth() + 1);
                nw.setDate(1);
              %>
              <span><%= fmtDate(nw.toISOString().split('T')[0]) %></span>
            </div>
            <div class="gv-withdraw-icon">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
            </div>
          </div>
        </div>

        <div class="gv-divider"></div>

        <!-- Sort Dropdown -->
        <div class="gv-sort-wrap">
          <select class="gv-sort-select">
            <option value="az">By A-Z</option>
            <option value="za">By Z-A</option>
          </select>
        </div>

        <!-- Student List -->
        <div class="gv-student-list" id="activeStudentList">
          <% if (students.length === 0) { %>
            <p class="muted" style="font-size:13px; padding:12px 0;">No students assigned yet.</p>
          <% } else { %>
            <% students.forEach((s, i) => { %>
              <div class="gv-student-row" id="student-row-<%= s.id %>" data-status="<%= s.status || 'active' %>">
                <span class="gv-student-num"><%= i + 1 %>.</span>
                <span class="gv-student-dot <%= s.balance < 0 ? 'dot-red' : s.balance >= 0 ? 'dot-green' : '' %>" id="dot-<%= s.id %>"></span>
                <a href="/students/view/<%= s.id %>" class="gv-student-name"><%= s.firstName %> <%= s.lastName %></a>
                <span class="gv-student-phone">(<%= (s.phone || '').replace('+998 ', '').replace('+998', '') %>)</span>
                <button class="gv-student-menu-btn" onclick="this.parentElement.classList.toggle('menu-open')">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="5" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="12" cy="19" r="1"/></svg>
                </button>
                <div class="gv-student-dropdown">
                  <a href="/students/view/<%= s.id %>" class="gv-student-dropdown-item">View</a>
                  <a href="/students/edit/<%= s.id %>" class="gv-student-dropdown-item">Edit</a>
                  <% if (s.status === 'inactive') { %>
                    <button class="gv-student-dropdown-item gv-sdd-action" onclick="groupStudentAction(event,'unfreeze','<%= s.id %>','<%= s.firstName + ' ' + s.lastName %>')">Unfreeze</button>
                  <% } else { %>
                    <button class="gv-student-dropdown-item gv-sdd-action" onclick="groupStudentAction(event,'freeze','<%= s.id %>','<%= s.firstName + ' ' + s.lastName %>')">Freeze</button>
                  <% } %>
                  <button class="gv-student-dropdown-item gv-sdd-danger" onclick="groupStudentAction(event,'remove','<%= s.id %>','<%= s.firstName + ' ' + s.lastName %>')">Remove from group (Archive)</button>
                </div>
              </div>
            <% }) %>
          <% } %>
        </div>

        <!-- Show/Hide archived students -->
        <div class="gv-archived-btn-wrap">
          <% if (archivedStudents && archivedStudents.length > 0) { %>
            <button class="gv-archived-btn" onclick="toggleArchivedStudents(this)" data-open="false">
              Show archived students (<%= archivedStudents.length %>)
            </button>
          <% } else { %>
            <button class="gv-archived-btn" disabled style="opacity:0.4;cursor:default;">No archived students</button>
          <% } %>
          <button class="gv-download-btn" title="Download">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
          </button>
        </div>

        <!-- Archived Students Section (hidden by default) -->
        <div class="gv-archived-list" id="archivedStudentList" style="display:none;">
          <div class="gv-archived-list-title">Archived students</div>
          <% if (archivedStudents && archivedStudents.length > 0) { %>
            <% archivedStudents.forEach(function(s) { %>
              <div class="gv-student-row gv-student-row--archived" id="student-row-<%= s.id %>">
                <span class="gv-student-dot dot-grey"></span>
                <span class="gv-student-name gv-student-name--archived"><%= s.firstName %> <%= s.lastName %></span>
                <span class="gv-student-phone">(<%= (s.phone || '').replace('+998 ', '').replace('+998', '') %>)</span>
                <button class="gv-student-menu-btn" onclick="this.parentElement.classList.toggle('menu-open')">
                  <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="5" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="12" cy="19" r="1"/></svg>
                </button>
                <div class="gv-student-dropdown">
                  <a href="/students/view/<%= s.id %>" class="gv-student-dropdown-item">View</a>
                  <button class="gv-student-dropdown-item gv-sdd-success" onclick="groupStudentAction(event,'restore','<%= s.id %>','<%= s.firstName + ' ' + s.lastName %>')">Restore to group</button>
                </div>
              </div>
            <% }) %>
          <% } %>
        </div>

        <div class="gv-divider"></div>

        <!-- Note Section -->
        <div class="gv-note-section">
          <div class="gv-note-bar"></div>
          <div class="gv-note-content">
            <span class="gv-note-placeholder">Note</span>
          </div>
          <div class="gv-note-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z"/><line x1="4" y1="22" x2="4" y2="15"/></svg>
          </div>
        </div>
      </div>

      <!-- ============ RIGHT PANEL ============ -->
      <div class="gv-right">
        <!-- Tabs -->
        <div class="gv-tabs">
          <button class="gv-tab active" data-gtab="attendance">Attendance</button>
          <button class="gv-tab" data-gtab="grade">Grade</button>
          <button class="gv-tab" data-gtab="online">Online</button>
          <button class="gv-tab" data-gtab="discounts">Discounts</button>
          <button class="gv-tab" data-gtab="exams">Exams</button>
          <button class="gv-tab" data-gtab="history">History</button>
          <button class="gv-tab" data-gtab="comments">Comments</button>
        </div>

        <!-- TAB: Attendance -->
        <div class="gv-gtab-content active" id="gtab-attendance">

        <!-- Attendance Section -->
        <div class="gv-attendance-section">
          <div class="gv-attendance-header">
            <h2>Attendance</h2>
            <div class="gv-attendance-nav">
              <% if (prevModule) { %>
                <a href="/groups/view/<%= group.id %>?module=<%= prevModule %>" class="gv-att-nav-btn gv-att-arrow" title="Previous module">&lsaquo;</a>
              <% } else { %>
                <span class="gv-att-nav-btn gv-att-arrow disabled">&lsaquo;</span>
              <% } %>
              <span class="gv-att-date-range gv-module-label"><%= moduleLabel %></span>
              <% if (nextModule) { %>
                <a href="/groups/view/<%= group.id %>?module=<%= nextModule %>" class="gv-att-nav-btn gv-att-arrow" title="Next module">&rsaquo;</a>
              <% } else { %>
                <span class="gv-att-nav-btn gv-att-arrow disabled">&rsaquo;</span>
              <% } %>
              <% if (totalModules > 1) { %>
                <select class="gv-att-month-select" onchange="if(this.value) window.location='/groups/view/<%= group.id %>?module='+this.value">
                  <% for (var m = 1; m <= totalModules; m++) { %>
                    <option value="<%= m %>" <%= m === currentModule ? 'selected' : '' %>>Module <%= m %></option>
                  <% } %>
                </select>
              <% } %>
            </div>
          </div>

          <% if (students.length === 0) { %>
            <p class="muted" style="font-size:13px;">Add students to track attendance.</p>
          <% } else if (lessonDates.length === 0 && allLessonDates.length === 0) { %>
            <p class="muted" style="font-size:13px;">Set a course with lessons/month, start date, and days to generate lesson dates.</p>
          <% } else if (lessonDates.length === 0) { %>
            <p class="muted" style="font-size:13px;">No lessons in this module. Use navigation to switch modules.</p>
          <% } else { %>
            <!-- Bulk actions -->
            <div class="gv-att-bulk-actions">
              <button class="gv-att-bulk-btn" onclick="bulkMarkAllPresent()" title="Mark all students present for today">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
                Mark all present today
              </button>
            </div>

            <div class="gv-attendance-table-wrap">
              <table class="gv-attendance-table" id="attendanceTable">
                <thead>
                  <tr>
                    <th class="gv-att-name-col">Name</th>
                    <% lessonDates.forEach(function(d) { %>
                      <%
                        var dd = String(d.getDate()).padStart(2, '0');
                        var mm = String(d.getMonth() + 1).padStart(2, '0');
                        var months = ['jan','feb','mar','apr','may','jun','jul','aug','sep','oct','nov','dec'];
                        var dateStr = d.getFullYear() + '-' + mm + '-' + dd;
                        var today = new Date();
                        var isToday = d.toDateString() === today.toDateString();
                        var isFuture = d > today && !isToday;
                      %>
                      <th class="gv-att-date-col <%= isToday ? 'today' : '' %> <%= isFuture ? 'future' : '' %>" data-date="<%= dateStr %>">
                        <span class="gv-att-day"><%= d.getDate() %> <%= months[d.getMonth()] %></span>
                        <% if (isToday) { %><span class="gv-att-today-dot"></span><% } %>
                      </th>
                    <% }) %>
                  </tr>
                </thead>
                <tbody>
                  <% students.forEach(function(s, si) { %>
                    <%
                      // Determine student's join date for this group
                      var joinRaw = s.joinedAt ? (typeof s.joinedAt === 'string' ? s.joinedAt : new Date(s.joinedAt).toISOString()) : null;
                      var studentJoinDate = joinRaw ? new Date(joinRaw.split('T')[0] + 'T00:00:00') : null;
                    %>
                    <tr data-student-id="<%= s.id %>">
                      <td class="gv-att-name-cell">
                        <div class="gv-att-avatar gv-att-avatar-<%= (si % 6) + 1 %>"><%= s.firstName.charAt(0) %><%= s.lastName.charAt(0) %></div>
                        <span class="gv-att-student-name"><%= s.firstName %> <%= s.lastName %></span>
                      </td>
                      <% lessonDates.forEach(function(d) { %>
                        <%
                          var dd2 = String(d.getDate()).padStart(2,'0');
                          var mm2 = String(d.getMonth()+1).padStart(2,'0');
                          var dateStr2 = d.getFullYear()+'-'+mm2+'-'+dd2;
                          var studentStatus = attendanceMap[dateStr2] && attendanceMap[dateStr2][s.id];
                          // Check if lesson is before student joined
                          var isBeforeJoin = studentJoinDate && d < studentJoinDate;
                          var today2 = new Date();
                          var isToday2 = d.toDateString() === today2.toDateString();
                        %>
                        <% if (isBeforeJoin) { %>
                          <td class="gv-att-check-cell gv-att-disabled" data-date="<%= dateStr2 %>" title="Student not yet enrolled">
                            <span class="gv-att-na"></span>
                          </td>
                        <% } else { %>
                          <td class="gv-att-check-cell <%= isToday2 ? 'gv-att-today-cell' : '' %>"
                              data-date="<%= dateStr2 %>"
                              data-student="<%= s.id %>"
                              data-status="<%= studentStatus || '' %>"
                              onclick="cycleAttendance(this)">
                            <% if (studentStatus === 'was') { %>
                              <span class="gv-att-status gv-att-present" title="Present">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg>
                              </span>
                            <% } else if (studentStatus === 'not') { %>
                              <span class="gv-att-status gv-att-absent" title="Absent">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                              </span>
                            <% } else { %>
                              <span class="gv-att-circle" title="Click to mark"></span>
                            <% } %>
                          </td>
                        <% } %>
                      <% }) %>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
          <% } %>
        </div>
        </div><!-- /gtab-attendance -->

        <!-- TAB: Grade (placeholder) -->
        <div class="gv-gtab-content" id="gtab-grade">
          <div class="gv-placeholder-panel">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
            <h3>Grade</h3>
            <p>Student grades and assessments will appear here.</p>
          </div>
        </div>

        <!-- TAB: Online (placeholder) -->
        <div class="gv-gtab-content" id="gtab-online">
          <div class="gv-placeholder-panel">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
            <h3>Online</h3>
            <p>Online session data will appear here.</p>
          </div>
        </div>

        <!-- TAB: Discounts (placeholder) -->
        <div class="gv-gtab-content" id="gtab-discounts">
          <div class="gv-placeholder-panel">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><line x1="19" y1="5" x2="5" y2="19"/><circle cx="6.5" cy="6.5" r="2.5"/><circle cx="17.5" cy="17.5" r="2.5"/></svg>
            <h3>Discounts</h3>
            <p>Student discount information will appear here.</p>
          </div>
        </div>

        <!-- TAB: Exams (placeholder) -->
        <div class="gv-gtab-content" id="gtab-exams">
          <div class="gv-placeholder-panel">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
            <h3>Exams</h3>
            <p>Exam results and schedules will appear here.</p>
          </div>
        </div>

        <!-- TAB: History (placeholder) -->
        <div class="gv-gtab-content" id="gtab-history">
          <div class="gv-placeholder-panel">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
            <h3>History</h3>
            <p>Group activity history will appear here.</p>
          </div>
        </div>

        <!-- TAB: Comments (placeholder) -->
        <div class="gv-gtab-content" id="gtab-comments">
          <div class="gv-placeholder-panel">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
            <h3>Comments</h3>
            <p>Group comments and notes will appear here.</p>
          </div>
        </div>

      </div>
    </div>

  </div>

  <!-- ============ EDIT GROUP MODAL ============ -->
  <div class="gv-modal-overlay" id="editGroupModal" style="display:none;">
    <div class="gv-modal-container">
      <div class="gv-modal-header">
        <h2>Edit group</h2>
        <button class="gv-modal-close" onclick="closeEditGroupModal()">&times;</button>
      </div>
      <form action="/groups/edit/<%= group.id %>" method="POST" class="gv-modal-form">
        <!-- Group Name -->
        <div class="gv-form-group">
          <input type="text" name="name" value="<%= group.name %>" class="gv-input" required>
          <span class="gv-input-indicator"></span>
        </div>

        <!-- Select course -->
        <div class="gv-form-group">
          <label>Select course</label>
          <select name="courseId" class="gv-select">
            <option value="">-- Select Course --</option>
            <% courses.forEach(function(c) { %>
              <option value="<%= c.id %>" <%= group.courseId === c.id ? 'selected' : '' %>><%= c.name %></option>
            <% }) %>
          </select>
        </div>

        <!-- Select teacher -->
        <div class="gv-form-group">
          <label>Select teacher</label>
          <select name="teacherId" class="gv-select">
            <option value="">-- Select Teacher --</option>
            <% teachers.forEach(function(t) { %>
              <option value="<%= t.id %>" <%= group.teacherId === t.id ? 'selected' : '' %>><%= t.firstName.toUpperCase() %> <%= t.lastName.toUpperCase() %></option>
            <% }) %>
          </select>
        </div>

        <!-- Days -->
        <div class="gv-form-group">
          <label>Exact days</label>
          <div class="gv-days-grid">
            <% daysOfWeek.forEach(function(day) { %>
              <label class="gv-day-checkbox">
                <input type="checkbox" name="days" value="<%= day %>" <%= group.days && group.days.includes(day) ? 'checked' : '' %>>
                <span class="gv-day-label"><%= day %></span>
              </label>
            <% }) %>
          </div>
        </div>

        <!-- Select room -->
        <div class="gv-form-group">
          <label>Select room</label>
          <select name="roomId" class="gv-select">
            <option value="">-- Select Room --</option>
            <% rooms.forEach(function(room) { %>
              <option value="<%= room %>" <%= group.room === room ? 'selected' : '' %>><%= room %></option>
            <% }) %>
          </select>
        </div>

        <!-- Lesson start time -->
        <div class="gv-form-group">
          <label>Lesson start time</label>
          <div class="gv-time-input-wrap">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
            <select name="startTime" class="gv-time-input">
              <option value="">-- Select Time --</option>
              <% timeSlots.forEach(function(slot) { %>
                <option value="<%= slot %>" <%= group.startTime === slot ? 'selected' : '' %>><%= slot %></option>
              <% }) %>
            </select>
          </div>
        </div>

        <!-- Group start date -->
        <div class="gv-form-group">
          <label>Group start date</label>
          <div class="gv-date-input-wrap">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
            <input type="date" name="startDate" value="<%= group.startDate || '' %>">
          </div>
        </div>

        <!-- Group end date -->
        <div class="gv-form-group">
          <label>Group end date</label>
          <div class="gv-date-input-wrap">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
            <input type="date" name="endDate" value="<%= computedEndDate || '' %>">
          </div>
        </div>

        <div class="gv-form-group">
          <label>Status</label>
          <select name="status" class="gv-select">
            <option value="active" <%= group.status === 'active' ? 'selected' : '' %>>Active</option>
            <option value="completed" <%= group.status === 'completed' ? 'selected' : '' %>>Completed</option>
            <option value="archived" <%= group.status === 'archived' ? 'selected' : '' %>>Archived</option>
          </select>
        </div>

        <button type="submit" class="gv-submit-btn">Save Changes</button>
      </form>
    </div>
  </div>

</main>

<script>
// Group detail tabs
document.querySelectorAll('.gv-tab[data-gtab]').forEach(function(tab) {
  tab.addEventListener('click', function() {
    document.querySelectorAll('.gv-tab[data-gtab]').forEach(function(t) { t.classList.remove('active'); });
    document.querySelectorAll('.gv-gtab-content').forEach(function(c) { c.classList.remove('active'); });
    tab.classList.add('active');
    var panel = document.getElementById('gtab-' + tab.dataset.gtab);
    if (panel) panel.classList.add('active');
  });
});

function openEditGroupModal() {
  document.getElementById('editGroupModal').style.display = 'flex';
  document.body.style.overflow = 'hidden';
}
function closeEditGroupModal() {
  document.getElementById('editGroupModal').style.display = 'none';
  document.body.style.overflow = '';
}
document.addEventListener('click', function(e) {
  if (e.target.id === 'editGroupModal') closeEditGroupModal();
});
document.addEventListener('keydown', function(e) {
  if (e.key === 'Escape') closeEditGroupModal();
});

// Close student dropdown on outside click
document.addEventListener('click', function(e) {
  document.querySelectorAll('.gv-student-row.menu-open').forEach(function(row) {
    if (!row.contains(e.target)) row.classList.remove('menu-open');
  });
});

// Toggle archived students section
function toggleArchivedStudents(btn) {
  var list = document.getElementById('archivedStudentList');
  var open = btn.getAttribute('data-open') === 'true';
  if (open) {
    list.style.display = 'none';
    btn.setAttribute('data-open', 'false');
    btn.textContent = btn.textContent.replace('Hide', 'Show');
  } else {
    list.style.display = 'block';
    btn.setAttribute('data-open', 'true');
    btn.textContent = btn.textContent.replace('Show', 'Hide');
  }
}

// 3-dot actions on group detail page
var GROUP_ID = '<%= group.id %>';
function groupStudentAction(e, action, studentId, name) {
  e.stopPropagation();
  var row = document.getElementById('student-row-' + studentId);
  var messages = {
    remove: 'Remove "' + name + '" from this group? They will be archived.',
    freeze: 'Freeze "' + name + '"? Their status becomes inactive.',
    unfreeze: 'Unfreeze "' + name + '"?',
    restore: 'Restore "' + name + '" back to active in this group?'
  };
  if (!confirm(messages[action] || 'Are you sure?')) return;

  var urlMap = {
    remove:   '/groups/' + GROUP_ID + '/remove-student/' + studentId,
    freeze:   '/groups/' + GROUP_ID + '/freeze-student/' + studentId,
    unfreeze: '/groups/' + GROUP_ID + '/unfreeze-student/' + studentId,
    restore:  '/groups/' + GROUP_ID + '/restore-student/' + studentId
  };

  fetch(urlMap[action], {
    method: 'POST',
    headers: { 'X-Requested-With': 'XMLHttpRequest' }
  })
  .then(function(r) { return r.json(); })
  .then(function(data) {
    if (!data.success) return;
    if (action === 'remove') {
      // Move row to archived list
      if (row) {
        row.classList.add('gv-student-row--archived');
        var archivedList = document.getElementById('archivedStudentList');
        if (archivedList) archivedList.appendChild(row);
        // Update row's dropdown to show Restore only
        var dd = row.querySelector('.gv-student-dropdown');
        if (dd) {
          dd.innerHTML = '<a href="/students/view/' + studentId + '" class="gv-student-dropdown-item">View</a>' +
            '<button class="gv-student-dropdown-item gv-sdd-success" onclick="groupStudentAction(event,\'restore\',\'' + studentId + '\',\'' + name + '\')">Restore to group</button>';
        }
        // Update the archived btn label
        var arBtn = document.querySelector('.gv-archived-btn[data-open]');
        if (arBtn) {
          var count = archivedList ? archivedList.querySelectorAll('.gv-student-row--archived').length : 1;
          arBtn.removeAttribute('disabled');
          arBtn.style.opacity = '';
          arBtn.style.cursor = '';
          arBtn.setAttribute('onclick', 'toggleArchivedStudents(this)');
        }
      }
    } else if (action === 'restore') {
      // Move row back to active list
      if (row) {
        row.classList.remove('gv-student-row--archived');
        var activeList = document.getElementById('activeStudentList');
        if (activeList) activeList.appendChild(row);
        var dd2 = row.querySelector('.gv-student-dropdown');
        if (dd2) {
          dd2.innerHTML = '<a href="/students/view/' + studentId + '" class="gv-student-dropdown-item">View</a>' +
            '<a href="/students/edit/' + studentId + '" class="gv-student-dropdown-item">Edit</a>' +
            '<button class="gv-student-dropdown-item gv-sdd-action" onclick="groupStudentAction(event,\'freeze\',\'' + studentId + '\',\'' + name + '\')">Freeze</button>' +
            '<button class="gv-student-dropdown-item gv-sdd-danger" onclick="groupStudentAction(event,\'remove\',\'' + studentId + '\',\'' + name + '\')">Remove from group (Archive)</button>';
        }
      }
    } else if (action === 'freeze') {
      if (row) {
        row.setAttribute('data-status', 'inactive');
        row.style.opacity = '0.65';
        var dot = document.getElementById('dot-' + studentId);
        if (dot) { dot.className = 'gv-student-dot dot-grey'; }
        var dd3 = row.querySelector('.gv-student-dropdown');
        if (dd3) {
          // swap freeze → unfreeze in dropdown
          var freezeBtn = dd3.querySelector('[onclick*="freeze"]');
          if (freezeBtn) {
            freezeBtn.setAttribute('onclick', 'groupStudentAction(event,\'unfreeze\',\'' + studentId + '\',\'' + name + '\')');
            freezeBtn.textContent = 'Unfreeze';
          }
        }
      }
    } else if (action === 'unfreeze') {
      if (row) {
        row.setAttribute('data-status', 'active');
        row.style.opacity = '';
        var dd4 = row.querySelector('.gv-student-dropdown');
        if (dd4) {
          var unfreezeBtn = dd4.querySelector('[onclick*="unfreeze"]');
          if (unfreezeBtn) {
            unfreezeBtn.setAttribute('onclick', 'groupStudentAction(event,\'freeze\',\'' + studentId + '\',\'' + name + '\')');
            unfreezeBtn.textContent = 'Freeze';
          }
        }
      }
    }
  })
  .catch(function() { window.location.reload(); });
}

// === Click-to-cycle attendance (AJAX, no page reload) ===
// Cycle: empty -> was -> not -> empty
function cycleAttendance(cell) {
  var currentStatus = cell.getAttribute('data-status') || '';
  var nextStatus;
  if (currentStatus === '') nextStatus = 'was';
  else if (currentStatus === 'was') nextStatus = 'not';
  else nextStatus = 'clear'; // will clear the status

  var studentId = cell.getAttribute('data-student');
  var date = cell.getAttribute('data-date');

  // Optimistically update UI
  updateCellUI(cell, nextStatus === 'clear' ? '' : nextStatus);
  cell.setAttribute('data-status', nextStatus === 'clear' ? '' : nextStatus);

  // Send AJAX request
  var xhr = new XMLHttpRequest();
  xhr.open('POST', '/groups/<%= group.id %>/attendance', true);
  xhr.setRequestHeader('Content-Type', 'application/json');
  xhr.setRequestHeader('X-Requested-With', 'XMLHttpRequest');
  xhr.onreadystatechange = function() {
    if (xhr.readyState === 4 && xhr.status !== 200) {
      // Revert on error
      updateCellUI(cell, currentStatus);
      cell.setAttribute('data-status', currentStatus);
    }
  };
  xhr.send(JSON.stringify({ date: date, studentId: studentId, status: nextStatus }));
}

function updateCellUI(cell, status) {
  if (status === 'was') {
    cell.innerHTML = '<span class="gv-att-status gv-att-present" title="Present"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"/></svg></span>';
  } else if (status === 'not') {
    cell.innerHTML = '<span class="gv-att-status gv-att-absent" title="Absent"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></span>';
  } else {
    cell.innerHTML = '<span class="gv-att-circle" title="Click to mark"></span>';
  }
}

// === Bulk mark all present for today ===
function bulkMarkAllPresent() {
  var today = new Date();
  var dd = String(today.getDate()).padStart(2, '0');
  var mm = String(today.getMonth() + 1).padStart(2, '0');
  var todayStr = today.getFullYear() + '-' + mm + '-' + dd;

  // Find all cells for today
  var cells = document.querySelectorAll('.gv-att-check-cell[data-date="' + todayStr + '"][data-student]');
  if (cells.length === 0) return;

  var studentIds = [];
  cells.forEach(function(cell) {
    studentIds.push(cell.getAttribute('data-student'));
    updateCellUI(cell, 'was');
    cell.setAttribute('data-status', 'was');
  });

  // Send bulk AJAX
  var xhr = new XMLHttpRequest();
  xhr.open('POST', '/groups/<%= group.id %>/attendance/bulk', true);
  xhr.setRequestHeader('Content-Type', 'application/json');
  xhr.setRequestHeader('X-Requested-With', 'XMLHttpRequest');
  xhr.send(JSON.stringify({ date: todayStr, studentIds: studentIds, status: 'was' }));
}
</script>
<%- include('../partials/footer') %>
